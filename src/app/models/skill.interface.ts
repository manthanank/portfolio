export interface Skill {
  name: string;
  level: number; // 1-5 scale
}

export interface TimelineItem {
  title: string;
  company: string;
  year: string;
  description: string;
  icon: string;
  iconColor: string;
}
